import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App loads', (WidgetTester tester) async {
    // Placeholder: app requires DB init which is not available in test env
  });
}
