name: humanopen
description: "Standalone personal AI appliance - private, persistent, unlimited"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.4.2
  path_provider: ^2.1.5
  path: ^1.9.1
  http: ^1.3.0
  llama_flutter_android:
    path: plugins/llama_flutter_android

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
